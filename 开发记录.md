# 开发记录

> 本文件按日期分段记录项目的功能开发与整改过程，供后续维护追溯。

---

## 2026-09-14 · 模型热切换 / 录像自选删除 / 移除轨迹绘制

- **提交**：`09f9a7b`（main，13 文件，+613 / −275）
- **测试**：单元测试 220 → 245，`pytest -q` 全绿（exit 0）

### 一、需求背景

1. **模型热切换**：网页端希望随时手动切换 YOLO 检测模型（开放词汇 `yolov8x-worldv2` 与常规 `yolov8m` / `yolov8n`，或项目根目录任意 `.pt`），无需重启服务。
2. **录像自选删除**：滚动录制的录像段此前只能等保留窗口自动清理，希望网页端能自选删除指定段。
3. **移除轨迹绘制**：画面上按跟踪 ID 绘制的目标移动尾迹呈现为杂乱折线（乱线），影响观感，要求整体移除。

### 二、方案与实现

#### 1. 模型热切换（方案 A：请求线程内加载 + 原子换引用 + 标志位通知）

- `detector.py`
  - `_is_world_model(path)` / `_load_model(path)`：统一 ultralytics 加载入口（world 模型自动 `set_classes(DETECTION_CLASSES)`），是唯一触碰 ultralytics 的地方，便于测试打桩。
  - `list_available_models(search_dir=None)`：非递归扫描项目根 `*.pt`（天然排除 `weights/` 子目录），返回 `[{name, is_world, active}]` 并按名排序；`search_dir` 仅供测试注入。
  - `switch_model(name, search_dir=None)`：白名单校验（必须命中根目录 basename 集合）→ 锁内加载新模型 → 原子替换 `_model` → 同步 `settings.model_path` / `is_world_model` → 置 `_model_reload_requested`。**加载失败抛异常，旧模型与 settings 完整保留**。
  - `apply_model_reload()`：采集循环发现标志后重取模型、`request_tracker_reset()` 重建 ByteTrack 会话、清空旧模型检测框、`reset_source_metrics()` 清零跨模型累计指标、复位标志。
  - **关键不变量**：`get_model()` 在 `_model` 非空时不加锁直接返回，因此 `switch_model` 在锁内加载不会阻塞每帧绘制的读路径，切换过程画面不中断。
- `routes.py`：新增 `POST /set_model`（白名单失败 400、加载失败 500，两者均不影响画面）；`/model_info` 增量返回 `available_models`。
- 前端 `templates/index.html`：运行参数面板"模型"行由只读文本改为 `<select id="modelSelect">`；新增 `renderModelSelect` / `onModelChange`（`escapeHtml` 转义、`modelSwitching` 互斥防并发、失败双路径回滚选中项）。

#### 2. 录像自选删除

- `recorder.py`：新增 `POST /recordings/delete`。安全边界三重：`os.path.basename` + `_CLIP_NAME_RE` 严格正则（防路径穿越）+ 活动段（`_active_segment_name`）拒绝删除；不存在 404；`FileNotFoundError` 视为幂等成功（与滚动清理线程竞态）；删除后同步 `_duration_cache.pop`。
- 前端：录像列表每段加红色"删除"按钮 + 原生 `confirm` 确认 + `deleteClip()`（若删除的是当前回放段先 `backToLive()`）+ `fetchRecording()` 刷新段数/总大小/列表。

#### 3. 移除轨迹绘制

- 删除 `tracks.py` 与 `tests/test_tracks.py`。
- `detector.py` 移除 `import tracks` 与循环内绘制旁路块；`config.py` 移除 `trail_enabled` / `trail_max_points` 字段及 `TRAIL_ENABLED` / `TRAIL_MAX_POINTS` 环境变量；`tests/conftest.py` 移除 `_isolate_state` 中 tracks 快照/还原；`app.py` 模块清单与 `使用指南.md` 同步清理。全仓 grep 确认无残留引用、无 ImportError、无死代码。

### 三、验证

- **单元测试**：220 → 245 全绿。新增模型切换 18 例（`TestListAvailableModels` / `TestSwitchModelValidation` / `TestSwitchModelSuccess` / `TestApplyModelReload`）、录像删除 10 例（`TestRecordingsDelete`）、审计回归 7 例；移除轨迹 10 例。全部走 `search_dir` 注入 + `_load_model` 打桩，**单测不真实加载权重**。
- **后端集成（真实加载权重）**：`/model_info` 的 `available_models` 正确；`yolov8n → yolov8m → yolov8x-worldv2` 真实切换成功（worldv2 `open_vocab=True`、`total_classes=124`）；非法名 / 路径穿越 / 子目录引用 / 缺字段均 400；GET 405。脚本退出码 0。
- **浏览器 E2E**：模型下拉框正确渲染 3 个选项、切换 toast 成功、Console 无 JS 报错；尾迹折线完全消失；每段删除按钮可用，删除后 confirm + toast + 列表行数减 1。

### 四、代码审计与修复

CodeReview 结论：**无阻断 / 高危**。3 个中危已修复并补回归测试：

1. **非字符串 / 非 dict JSON 输入致 500**：`/set_model` 与 `/recordings/delete` 原 `(x or '').strip()` 兜不住非字符串，现做类型收敛返回 400（沿用 `zones.py` 既有加固口径）。
2. **切换模型后 metrics 跨模型混算**：`apply_model_reload` 原仅清 `prev_tracks`，现改用 `reset_source_metrics()` 与换源同口径清零 `detections_total` / `class_counts` / `id_switches`。
3. **删除与滚动清理竞态误报 500**：`os.remove` 抛 `FileNotFoundError`（段已被清理线程删除）原返回 500，现视为幂等成功 200。

低危（另开跟进，未随本次修复）：

4. 模型白名单为动态目录扫描，根目录任意 `.pt` 均可被一键加载（`torch.load` 反序列化面）；`--host 0.0.0.0` 时 `/set_model`、`/recordings/delete` 无鉴权。建议后续改固定白名单或加鉴权。
5. 切换瞬间新旧模型同驻显存（方案 A 固有代价），小显存卡切到大 world 模型有 OOM 可能（会被 `detection_loop` 兜底重连，不崩服务）。
6. `YOLO_MODEL` 指向项目根之外时，下拉框无 `active` 项，会出现空选中显示。

### 五、影响面与兼容

- 既有 8 个路由的 URL / 方法 / JSON 字段契约零破坏；`/model_info` 仅增量新增 `available_models`。
- 移除 `TRAIL_ENABLED` / `TRAIL_MAX_POINTS` 环境变量（不再读取）。
- `使用指南.md` 同步更新：功能列表、环境变量表、5.1 节、目录树、模块关系、测试数 245。
