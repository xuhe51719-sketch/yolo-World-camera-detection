# YOLOv8 实时目标检测系统 · 使用指南

本项目是一个基于 YOLOv8 的实时目标检测系统：从本地摄像头或手机 IP 摄像头读取画面，用神经网络实时框出并识别画面中的物体，通过浏览器网页展示检测结果（带检测框的视频、类别、置信度、性能指标等）。

## 功能概览

- **124 类开放词汇检测**：默认使用 YOLO-World 开放词汇模型（`yolov8x-worldv2.pt`），在标准 COCO 80 类之外，还能识别饮水机、垃圾桶、插线板、显示器、眼镜、保温杯等 44 种日常物品，共 124 类。完整类别表定义在 `config_classes.py`（增删类别后重启服务生效，类别名必须为英文）。
- **双图像源**：默认自动选择本地摄像头；也可以把手机当摄像头用（手机装 IP Webcam / DroidCam 之类的推流 App），支持在网页上随时填写地址连接或切回本地。
- **多目标跟踪**：内置 ByteTrack 跟踪，每个物体有稳定的编号（ID），检测框不会逐帧闪烁。
- **网页前端**：实时视频画面、检测结果列表、性能指标（帧率/推理耗时/ID 切换等）、画面方向校正（旋转/镜像）。
- **滚动录制与倍速回放**（默认开启）：持续录制检测画面，每段 60 秒、最多滚动保留约 1 小时（自动删最旧段），磁盘剩余 <2GB 自动停录；网页上可选段回放，支持 0.5 / 1 / 1.5 / 2 倍速。
- **录像自选删除**：录像回放列表每段带“删除”按钮，确认后立即删除该段（正在录制的活动段不可删）。
- **模型热切换**：网页“运行参数”面板的模型下拉框可在项目根目录的 .pt 权重间随时切换（开放词汇 worldv2 / 常规 m / n），切换过程画面不中断。
- **多边形区域入侵报警**：在画面上手绘任意多边形警戒区域，人员与 10 类动物进入区域即触发报警（进入沿触发、滞留不重复、30 秒冷却），双端报警音 10 秒（网页 + 服务端电脑）并记录事件。
- **数据集工具链**：采集真实场景帧 → 人工标注 → 用 mAP 指标客观评估识别精度，以及参数扫描基准测试。

系统不支持（也无意支持）多路摄像头同时接入、在线修改检测类别等功能。类别调整请修改 `config_classes.py` 后重启服务。

---

## 1. 环境准备

要求：Windows / Linux，Python 3.8+（建议 3.10+）。以下命令均在**项目根目录**下、PowerShell 中执行。

### 1.1 创建并激活虚拟环境

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
```

> 如果提示"禁止运行脚本"，先执行一次 `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` 再激活。
> 之后每次打开新终端，都需要重新执行 `.venv\Scripts\Activate.ps1` 激活。

### 1.2 安装依赖

```powershell
pip install -r requirements.txt
```

依赖包括：flask、ultralytics、opencv-python、numpy、torch。

### 1.3 GPU 版 PyTorch（可选）

`pip` 默认安装的是 **CPU 版** torch，推理会明显偏慢。如果你有 NVIDIA 显卡，请按本机 CUDA 版本从 PyTorch 官方索引安装对应构建，例如 CUDA 12.1：

```powershell
pip install torch --index-url https://download.pytorch.org/whl/cu121
```

其他 CUDA 版本把结尾的 `cu121` 换成对应版本号即可（如 `cu118`）；全部可用版本见官方页面 <https://pytorch.org/get-started/locally/>。装完可用下面命令验证：

```powershell
python -c "import torch; print(torch.cuda.is_available())"
```

输出 `True` 即 GPU 可用。程序启动时会自动检测：有 CUDA 就用 GPU 并默认开启 FP16 半精度推理（更快、更省显存），没有就用 CPU。

### 1.4 测试依赖（仅开发者需要）

```powershell
pip install -r requirements-dev.txt
```

内容只有 `pytest`，运行第 9 节的单元测试时才会用到。

---

## 2. 启动方式

### 2.1 最简单启动

```powershell
python app.py
```

启动后按提示打开浏览器访问 `http://localhost:5000` 即可。程序会先在后台连接图像源、加载模型并跑通第一帧检测（最多等 30 秒），再启动网页服务。按 `Ctrl+C` 停止，程序会优雅释放摄像头。

### 2.2 命令行参数

| 参数 | 默认 | 说明 |
|---|---|---|
| `--url` | 无（用本地摄像头） | 手机 IP 摄像头流地址，如 `http://192.168.1.100:8080/video` |
| `--host` | `127.0.0.1` | 监听地址。**默认只允许本机访问**；局域网内其他设备（如手机浏览器）要查看，需显式传 `0.0.0.0` |
| `--port` | `5000` | Web 服务端口 |

示例：

```powershell
# 直接用手机号当摄像头
python app.py --url http://192.168.1.100:8080/video

# 让局域网内其他设备也能打开网页（如用手机浏览器看结果）
python app.py --host 0.0.0.0

# 5000 端口被占用时换端口
python app.py --port 5001
```

> 命令行参数优先于环境变量：两者同时设置时以命令行为准。
> 监听 `0.0.0.0` 时，终端会打印一条 `[WARN]` 提示，且 Windows 防火墙首次可能弹窗请求放行，请允许。

### 2.3 环境变量一览

所有环境变量在 `config.py` 中统一读取，以下为逐项说明（括号内为默认值）：

| 环境变量 | 默认值 | 作用 |
|---|---|---|
| `YOLO_MODEL` | `yolov8x-worldv2.pt` | 模型文件路径。可换成 `yolov8m.pt` / `yolov8n.pt`（传统固定 80 类模型，速度快，适合 CPU 或显存紧张时应急），权重文件放项目根目录即可 |
| `YOLO_IMGSZ` | `640` | 检测输入分辨率。调大更准但更慢更费显存，调小相反 |
| `YOLO_HALF` | `1` | 是否启用 FP16 半精度（仅在 CUDA 可用时生效）。设 `0` 回退 FP32；半精度异常（画面不出框、报错）时可关掉 |
| `DETECTION_INTERVAL` | `2` | 隔帧推理间隔：每 N 帧才做一次推理，中间帧复用上次结果。增大可提速降负载，减小更实时但更耗资源；最小为 1（每帧都推理） |
| `PHONE_CAMERA_URL` | 空（用本地摄像头） | 手机流地址，效果等同 `--url`，网页里填的地址会覆盖它 |
| `PHONE_ROTATE` | `0` | 画面初始旋转角度（顺时针），可选 `0` / `90` / `180` / `270`；运行时也可在网页上改 |
| `PHONE_MIRROR` | 空 | 画面初始镜像方式：空（不镜像）/ `h`（左右镜像）/ `v`（上下镜像）；运行时也可在网页上改 |
| `FLASK_HOST` | `127.0.0.1` | 监听地址，效果等同 `--host` |
| `FLASK_PORT` | `5000` | 服务端口，效果等同 `--port` |
| `LOG_LEVEL` | `INFO` | 控制台日志级别，排障时可设 `DEBUG` 看更详细信息 |
| `RECORD_ENABLED` | `1` | 是否启用滚动录制。**默认开启，首次运行即会向磁盘写录像**，设 `0` 关闭 |
| `RECORD_DIR` | `recordings/` | 录制段存放目录（相对路径锚定项目根，也可填绝对路径） |
| `RECORD_FPS` | `15` | 录制节流帧率（写入段的最大帧率），调大更流畅但更占磁盘 |
| `RECORD_SEGMENT_S` | `60` | 每段时长（秒），到点自动关段开新段 |
| `RECORD_RETENTION_MIN` | `60` | 滚动保留时长（分钟），超出的最旧段自动删除（默认约 1 小时） |
| `ALERT_COOLDOWN_S` | `30` | 同一（区域, 类别）的报警冷却秒数，避免滞留目标重复刷屏 |
| `ALERT_SOUND_S` | `10` | 报警音播放时长（秒） |
| `ZONES_FILE` | `zones.json` | 警戒区域持久化文件路径（相对路径锚定项目根） |

PowerShell 中设置环境变量并启动的写法（当前窗口内有效）：

```powershell
$env:YOLO_MODEL="yolov8n.pt"
$env:FLASK_PORT="5001"
python app.py
```

### 2.4 几个固定的内置参数（代码写死，了解即可）

- 置信度阈值 `0.2`：低于该值的检测框会被丢弃。
- 帧龄控制：只展示最新画面，超过 0.5 秒的旧帧不会被显示，避免"延迟越滚越大"。
- 本地摄像头目标分辨率 1280×720、30 FPS（摄像头不一定支持，以实际为准）。
- 手机流打开/读取超时 5 秒，连接失败会指数退避自动重试（最长间隔 5 秒）。

---

## 3. 本地摄像头

不设任何地址时，程序默认使用本地摄像头，并**自动选优**：

1. 依次尝试索引 0、1、2 的默认后端，再尝试 DirectShow 后端；
2. 对每个能打开的摄像头试读 3 帧测速：首帧超过 0.5 秒的直接跳过；
3. 平均读帧快于 0.2 秒的立即采用，否则保留测速最快的一个作为备选。

终端日志会打印每个候选的实测结果（如 `测试摄像头: index=0, backend=DirectShow, 分辨率=1280x720, 平均读帧=0.033s`），选中的摄像头信息也可在网页"运行参数 → 图像源分辨率"和 `/source` 接口中看到。

**画面全黑？** 最常见原因是笔记本摄像头的**物理隐私快门/滑盖没打开**（屏幕上方镜头旁的小滑块），网页会弹出黑色遮罩并提示；其次检查镜头是否被遮挡、摄像头是否被其他程序（会议软件等）占用。

---

## 4. 手机流接入

### 4.1 准备工作

1. 手机安装推流 App（如 **IP Webcam**、**DroidCam**），打开并启动服务；
2. 确保手机和电脑连在**同一个 WiFi**；
3. 记下 App 显示的地址，形如 `http://192.168.1.100:8080`。

### 4.2 连接方式（三选一，效果相同）

```powershell
# 方式一：启动时指定
python app.py --url http://192.168.1.100:8080/video

# 方式二：环境变量
$env:PHONE_CAMERA_URL="http://192.168.1.100:8080/video"
python app.py

# 方式三：网页上填（服务运行中随时可换，见 5.4 节）
```

### 4.3 地址格式要求与自动纠错

- **仅允许 `http` / `https` / `rtsp` 三种协议**。填其他协议（或没有主机名、端口不是数字等）会被拒绝，页面弹窗给出中文错误，例如"不支持的推流协议 'ftp'，仅允许 http/https/rtsp"。
- **主机名格式**：只允许字母、数字、点(`.`)、连字符(`-`)和下划线(`_`)（手机推流 App 的设备名常含下划线，如 `http://my_phone:8080/video` 可以填）；含空格或连续点（`..`）的主机名会被拒绝。
- **自动纠错**（提交时先纠错再校验）：
  - 全角字符自动转半角（中文输入法下打的 `：／１９２` 等会被转成 `:/192`）；
  - 没写协议头自动补 `http://`（直接填 `192.168.1.100:8080/video` 也行）；
  - 填了 `https://` 会优先尝试对应的 `http://` 地址（手机推流 App 一般只提供 HTTP）；
  - HTTP 地址没带路径时，会**优先自动补 `/video` 尝试**（IP Webcam 等 App 主界面显示 `http://IP:8080`，视频流实际在 `/video`）。若原始地址打不开而候选地址成功，终端会打印"原始地址打不开，已自动改用: ..."。
- 连接期间程序会临时移除 `HTTP_PROXY` 等代理环境变量（避免代理软件把局域网流量劫走），连接结束后自动还原，无需手动处理。
- 手机流连不上时**不会**回退本地摄像头，而是持续重试手机流（避免画面被本地摄像头"误导"）。

---

## 5. 前端界面操作

浏览器打开 `http://localhost:5000`（局域网模式下为 `http://电脑IP:5000`），界面分四块：

### 5.1 视频画面（左上）

实时视频流，检测框直接画在画面上（框旁标签为"类别 置信度 #跟踪ID"）。左上角有 LIVE 角标；图像源未连接或画面全黑时，会弹出黑底警告遮罩并给出排查提示。
- **REC 徽标**：录制开启时左上角显示红色 REC 徽标，附当前段数与总大小；录制因故停止（如磁盘不足）时徽标置灰并显示停录原因。

### 5.2 实时统计与检测结果

- 视频下方 4 张统计卡：**检测目标数、最高置信度、类别种类、平均置信度**。
- 右侧"检测结果"面板（每 200ms 刷新）：按置信度从高到低列出每个检出框，显示中文名 + 英文名、跟踪编号、坐标和置信度进度条（≥0.7 绿 / ≥0.45 橙 / 更低红）。

### 5.3 性能指标与运行参数（右侧）

- **性能指标**（每 1s 刷新）：管线帧率（≥20 绿 / ≥10 橙 / 更低红）、平均推理耗时、P95 推理延迟、循环耗时、累计 ID 切换次数（越低跟踪越稳）、GPU 显存（仅 GPU 模式显示），以及推理/循环耗时的趋势曲线。
- **运行参数**：模型文件名（如 `yolov8x-worldv2.pt`）、推理设备（cuda:0 / cpu）、检测输入分辨率、置信度阈值、跟踪器（ByteTrack）、图像源分辨率、累计检出框数、运行时长。

### 5.4 图像源面板：连接 / 切换 / 重置

- 输入框填手机流地址 → 点"**连接手机摄像头**"：后台切换图像源并重建跟踪（约 2.5 秒后自动刷新状态）。地址不合法会弹窗提示被拒绝的原因。
- 点"**本地摄像头**"：清空地址、切回本地摄像头（相当于"重置"）。
- 面板副标题显示当前状态（手机流已连接 / 本地摄像头模式 / 连接中...），顶部状态条同步显示"检测中 · 手机摄像头 / 本地摄像头"或"图像源未连接"。

### 5.5 画面方向校正（即时生效，无需重连）

- **旋转**：`0°` / `90°` / `180°` / `270°` 四个按钮（顺时针方向），点一下立即生效，当前选中项高亮。
- **镜像**：单个按钮三态循环：**无 → 水平（左右翻转）→ 垂直（上下翻转）→ 无**，按钮文字实时显示当前状态。
- 检测框画在校正后的画面上，方向不对时随手点即可。
- **注意**：旋转/镜像方向变更后，已绘制的警戒区域会被**自动清空**（仅方向真正改变时才清，同值重设不受影响），需重新绘制——因为方向变了之后旧区域坐标的语义已失效。

### 5.6 区域入侵报警（警戒区域 / 事件 / 报警音）

只对**人员与 10 类动物**判定入侵：person、bird、cat、dog、horse、sheep、cow、elephant、bear、zebra、giraffe（定义在 `config_classes.py` 的 `ALERT_CLASSES`）。后台检查线程约 5Hz，取目标检测框底边中心点做点在多边形内判定（凹多边形也支持）。

**绘制警戒区域：**

1. 在"警戒区域"卡片点"**编辑区域**"进入绘制模式；
2. 在视频画面上**单击逐个添加顶点**；
3. **双击**（或点"**闭合当前区域**"）完成一个多边形（至少 3 个顶点）；
4. 可继续绘制下一个区域，最多 10 个；
5. 点"**保存区域**"提交生效。

区域列表支持**单个"删除"**与"**清除全部**"。保存后区域以归一化坐标持久化到 `zones.json`，重启服务自动加载；分辨率变化无需重画。回放中禁止编辑/保存区域（画面不同步，坐标会误导）。

**触发规则与报警反馈：**

- **进入沿触发**：目标从区外进入区内才触发，滞留在区内不重复报警；
- **冷却**：同一（区域, 类别）触发后 `ALERT_COOLDOWN_S`（默认 30 秒）内不重复报警；
- **事件**：每次入侵记录一条事件（时间/区域/类别/跟踪ID/置信度），"事件"卡片实时列出，页面顶部同步红色横幅闪烁；
- **双端报警音**（持续 `ALERT_SOUND_S`，默认 10 秒）：服务端电脑直接发声（Windows）；网页端用 Web Audio 合成警笛。

> ⚠️ **重点提示：浏览器报警音必须先手动解锁。** 受浏览器自动播放策略限制，网页要先点一次"事件"卡片上的"**🔕 点击启用报警音**"按钮（成功后变为"🔔 报警音已开启"），之后入侵事件才会发声。**未解锁时只有红色横幅视觉提示 + 服务端电脑发声**，浏览器不会响。
>
> 非 Windows 服务端不发声（仅记日志），不影响报警事件记录。

**手动测试报警：** 点"**手动触发一次测试报警**"按钮（对应 `POST /alert/test`），会写一条"测试"事件并两端响铃，用于验证报警链路是否通畅。

### 5.7 滚动录制与录像回放（默认开启）

**录制默认开启**：首次运行即会向 `recordings/` 目录写录像，不需要任何配置；启动时终端横幅会打印录制状态（如 `录像: 开启（保留约 60 分钟滚动，每段 60s，RECORD_ENABLED=0 可关闭）`）。不想录可设 `RECORD_ENABLED=0` 后重启。录制为旁路线程（按 `RECORD_FPS` 节流、队列只留最新帧），不会拖慢检测主流程；编码器三级降级：mp4v/.mp4 → MJPG/.avi → 停录并记录原因。

**滚动策略：** 每段 `RECORD_SEGMENT_S`（默认 60）秒，段名形如 `seg_20260831_120000.mp4`；总量超过 `RECORD_RETENTION_MIN`（默认 60 分钟）时自动删除最旧段，磁盘占用不随运行时间无限增长。

**磁盘预算：** 720p @ 15fps（mp4v）实测约 **0.7～1.3 GB/小时**；默认保留上限 60 分钟，即稳态占用约 0.7～1.3 GB。**磁盘剩余 <2GB 时自动停录**（避免写满系统盘），此时前端 REC 徽标置灰并显示停录原因，清理磁盘后重启服务恢复录制。

**回放操作：**

1. 在"录像回放"卡片中选择一个已关闭的录制段（正在写入的当前段不列出）；
2. 画面切换为回放流，出现"▶ 回放中"角标；
3. 可随时切换倍速：**0.5× / 1× / 1.5× / 2×**；
4. 点"**返回 LIVE**"回到实时画面。


---

## 6. 接口一览

服务共提供 15 个业务端点 + 1 个健康检查端点：

| 端点 | 方法 | 用途 |
|---|---|---|
| `/` | GET | 网页主界面 |
| `/video_feed` | GET | MJPEG 实时视频流（网页 `<img>` 直接引用） |
| `/detections` | GET | 最新检测结果列表（类别/置信度/跟踪ID/坐标，JSON） |
| `/model_info` | GET | 模型信息：模型文件名（如 `yolov8x-worldv2.pt`，不含目录路径）、置信度阈值、输入分辨率、是否开放词汇、类别表、摄像头信息、推理设备 |
| `/source` | GET | 当前图像源状态：配置/生效地址、连接状态、画面亮度、黑屏警告、方向设置 |
| `/metrics` | GET | 性能指标：管线帧率、循环/推理耗时（均值与 P95）、ID 切换、累计检出、类别统计、GPU 显存等 |
| `/set_orientation` | POST | 设置画面方向，JSON 体如 `{"rotate": 90, "mirror": "h"}`；rotate 仅接受 0/90/180/270，mirror 仅接受 ""/"h"/"v"，非法值忽略。**响应新增 `regions_cleared` 字段**：方向真正改变时为 `true`，表示警戒区域已被清空需重画 |
| `/set_source` | POST | 切换图像源，JSON 体如 `{"url": "http://192.168.1.100:8080/video"}`；url 留空切回本地摄像头；地址非法返回 400 和中文错误信息 |
| `/zones` | GET | 当前警戒区域列表（归一化坐标） |
| `/zones` | POST | 整体替换区域定义，JSON 体如 `{"regions": [{"name": "门口", "points": [[x,y],...]}]}`；约束：≤10 区域、每区域 ≥3 顶点、坐标∈[0,1]；校验失败返回 400 + 中文错误；成功后持久化到 `zones.json` |
| `/events` | GET | 入侵事件列表（最新在后）+ `alarm_active`（报警音是否正在播放） |
| `/recordings/status` | GET | 录制状态：是否启用、段数、总大小（MB）、磁盘剩余（GB）、停录原因 |
| `/recordings/clips` | GET | 已关闭录制段列表（文件名/开始时间戳/时长/大小），正在写入的活动段不列出 |
| `/recordings/stream` | GET | 回放指定段：`?clip=seg_日期_时间.mp4&speed=1`，服务端解码后以 MJPEG 流输出；speed 仅支持 0.5/1/1.5/2，文件名白名单校验防路径穿越，非法参数返回 400 |
| `/alert/test` | POST | 手动触发一次测试报警：写入一条"测试"事件并响铃（联调用） |
| `/health` | GET | 轻量健康检查（运维看门狗用）：返回服务是否运行、最近 3 秒内是否有新帧（`frame_fresh`）、帧龄、运行时长；不触碰模型和摄像头 |

---

## 7. 数据集工具链

用于建立自己的"黄金数据集"，客观评估识别精度。以下命令均在项目根目录运行。

### 7.1 采集帧：`tools/capture_frames.py`

从手机流采集真实场景图片。**`--url` 为必填参数**（工具只从手机流采集，不支持本地摄像头）。

```powershell
python tools\capture_frames.py --url http://192.168.1.100:8080/video --count 60 --interval 0.5
```

| 参数 | 默认 | 说明 |
|---|---|---|
| `--url` | **必填** | 手机流地址 |
| `--count` | 60 | 采集帧数，建议 50~200，场景越丰富越好 |
| `--interval` | 0.5 | 相邻两帧间隔秒数，移动手机拍不同场景时可调大 |
| `--out` | `dataset` | 输出目录 |

采集期间缓慢移动手机，覆盖不同场景和角度。图片存入 `dataset/images/`，并自动生成/更新 `dataset/data.yaml`（类别表与 `config_classes.py` 保持一致；若已有手工改过的 yaml 则不覆盖，改写为 `data.yaml.new` 供人工确认）。

### 7.2 人工标注

按 `dataset/README_标注流程.md` 操作：使用 X-AnyLabeling / Roboflow / labelImg 等工具，为每张图片生成一个同名 `.txt` 标注文件（YOLO 格式，放 `dataset/labels/`）。

### 7.3 精度评估：`tools/eval_dataset.py`

```powershell
python tools\eval_dataset.py --data dataset\data.yaml
python tools\eval_dataset.py --data dataset\data.yaml --imgsz 960 --conf 0.2
```

| 参数 | 默认 | 说明 |
|---|---|---|
| `--data` | `dataset/data.yaml` | 数据集描述文件 |
| `--model` | `yolov8x-worldv2.pt` | 评估用的模型 |
| `--imgsz` | 960 | 评估输入分辨率 |
| `--conf` | 0.2 | 置信度阈值 |
| `--out` | `evaluation_report.csv` | 结果追加写入该 CSV，便于多次实验对比 |

输出 mAP50-95 / mAP50 / mAP75 / Precision / Recall 总体指标及各类别明细表。

> **注意：当前 `dataset/labels/` 目录是空的（尚未完成任何人工标注）。** 此时运行评估会直接报错退出："labels/ 里没有标注文件。请先完成人工标注再评估"。请先完成 7.1、7.2 两步再评估。

### 7.4 参数扫描：`tools/sweep.py`

在不同 `imgsz × conf` 组合下实测推理速度与检出量，帮你量化"速度—检出"权衡、挑选合适配置：

```powershell
python tools\sweep.py --images dataset\images
python tools\sweep.py --images cam_test --imgsz 640,960 --conf 0.1,0.2,0.3
```

| 参数 | 默认 | 说明 |
|---|---|---|
| `--images` | `dataset/images` | 图片目录（采集的帧或任意测试图，最多取前 30 张） |
| `--imgsz` | `640,800,960` | 逗号分隔的分辨率列表 |
| `--conf` | `0.1,0.2,0.3,0.4` | 逗号分隔的置信度阈值列表 |
| `--model` | `yolov8x-worldv2.pt` | 模型路径 |
| `--runs` | 1 | 每个组合跑的图片轮数，图少时可调大取平均 |
| `--out` | `benchmark_results.csv` | 结果 CSV |

解读建议：选满足帧率需求的最大 `imgsz`；`conf` 从检出量与误报的平衡中挑选。

---

## 8. 运行测试

项目自带 245 个单元测试（覆盖路由、摄像头地址纠错、检测逻辑、评估工具，以及滚动录制/回放/删除、模型热切换、区域入侵报警与新端点回归），先装测试依赖再运行：

```powershell
pip install -r requirements-dev.txt
python -m pytest
```

测试不依赖真实摄像头与 GPU，全部使用桩/假模型，几秒内跑完。只想看收集到哪些用例可加 `--collect-only -q`。

---

## 9. 常见问题排障

### 本地摄像头打不开 / 没有画面

1. **画面全黑**：先检查笔记本摄像头的物理隐私快门/滑盖是否打开（最常见原因），其次看镜头是否被遮挡。
2. **日志反复打印"没有可用的图像源，即将重试..."**：摄像头可能被其他程序（视频会议软件等）占用，关闭占用程序后重试；程序会以指数退避自动重试（最长 5 秒一次），无需手动重启。
3. 终端日志会打印每个候选摄像头的测试结果，可据此判断哪个索引/后端可用。

### 手机流连不上

1. 确认手机与电脑在**同一 WiFi**，手机推流 App 的服务已开启；
2. 地址带不带 `/video` 都可以（程序会自动补全尝试），但请确认 IP 和端口正确；
3. 若填的地址被拒绝，按弹窗的中文提示修正（仅支持 http/https/rtsp，且必须有合法主机名和端口）；
4. 开了代理软件（Clash 等）一般无碍——程序连接摄像头时会临时移除代理环境变量；若仍连不上，可临时关闭代理再试；
5. `--host 0.0.0.0` 模式下，首次启动若 Windows 防火墙弹窗请点"允许访问"，否则局域网设备打不开网页；
6. 手机端能连但一直黑屏：检查手机摄像头是否被遮挡、App 是否拿到相机权限，或换个推流 App。

### 帧率低 / 卡顿

1. 打开网页右侧"性能指标"面板，看**管线帧率**和**推理耗时**；
2. 确认在用 GPU 推理（运行参数面板的"推理设备"应为 `cuda:0`）。CPU 上跑默认的 `yolov8x-worldv2.pt` 本来就慢；
3. 降低输入分辨率：`$env:YOLO_IMGSZ="480"` 后重启；
4. CPU 环境或显卡较弱时换小模型：`$env:YOLO_MODEL="yolov8n.pt"`（注意换常规模型后只支持固定 80 类）；
5. 也可用 `tools/sweep.py` 实测各配置的速度后按需设置 `YOLO_IMGSZ`。
   系统已内置隔帧检测、流缓冲排空等优化，正常情况下延迟不会随运行时间累积。

### GPU 显存不足（CUDA out of memory）

1. 降低输入分辨率：`$env:YOLO_IMGSZ="480"`（默认 640）；
2. 确认半精度已开启（CUDA 下默认开启，`YOLO_HALF=1`），可显著省显存；
3. 换更小的模型：`$env:YOLO_MODEL="yolov8m.pt"` 或 `yolov8n.pt`；
4. 关闭其他占用显存的程序（游戏、其他推理进程等）。

### 端口 5000 被占用

```powershell
python app.py --port 5001
# 或
$env:FLASK_PORT="5001"
python app.py
```

### 局域网设备打不开网页

默认只监听 `127.0.0.1`（仅本机），需要显式放开：

```powershell
python app.py --host 0.0.0.0
```

然后用电脑在局域网内的 IP 访问（如 `http://192.168.1.50:5000`），并留意防火墙放行。

### 想看到更详细的日志

```powershell
$env:LOG_LEVEL="DEBUG"
python app.py
```

---

## 10. 项目结构

```
├── app.py                 # 程序入口：命令行参数解析、启动后台检测线程、Flask 服务
├── config.py              # 集中配置：所有环境变量读取与默认值、路径锚定
├── config_classes.py      # 124 类开放词汇检测类别表（单一来源，服务与工具共享）
├── state.py               # 共享运行时状态（最新帧 / 检测结果 / 摄像头信息 / 方向 / 入侵事件）
├── metrics.py             # 量化指标采集（耗时窗口 / ID 切换 / 类别统计）
├── camera.py              # 摄像头打开 / 自动选优 / 手机流重连 / 地址纠错 / 画面方向
├── detector.py            # 模型惰性单例与热切换 / 后台采集检测循环 / 检测框绘制（联动录制入队）
├── recorder.py            # 滚动录制（60秒/段、滚动保留、磁盘保护）/ 回放与删除路由（倍速、防路径穿越）
├── zones.py               # 多边形区域入侵报警（归一化区域 / 进入沿触发 / 事件 / 报警音 / 持久化）
├── routes.py              # Flask 路由（9 个业务端点 + /health 健康检查）
├── templates/
│   └── index.html         # 前端页面（视频 / 统计 / 指标 / 图像源与方向控制 / 区域绘制 / 事件与回放）
├── tools/
│   ├── capture_frames.py  # 从手机流采集场景帧，建立评估数据集
│   ├── eval_dataset.py    # 黄金数据集 mAP 精度评估
│   └── sweep.py           # imgsz × conf 参数扫描基准测试
├── tests/                 # 245 个单元测试（路由 / 摄像头 / 检测 / 评估工具 / 录制 / 模型切换 / 区域报警）
├── dataset/               # 数据集：images/ 采集图、labels/ 标注、data.yaml、标注流程说明
├── recordings/            # 滚动录制段存放目录（运行时自动生成，已入 .gitignore）
├── zones.json             # 警戒区域持久化文件（保存区域后自动生成，已入 .gitignore）
├── weights/clip/          # YOLO-World 依赖的 CLIP 文本编码器权重（ViT-B-32）
├── yolov8x-worldv2.pt     # 默认模型：开放词汇检测
├── yolov8m.pt / yolov8n.pt # 备选常规模型（固定 80 类，更快更省）
├── requirements.txt       # 运行依赖
└── requirements-dev.txt   # 测试依赖（pytest）
```

模块关系：`app.py` 启动时读取 `config.py` 配置、拉起 `detector.py` 的检测线程；检测线程经 `camera.py` 读画面、把结果写入 `state.py` 与 `metrics.py`，同时把帧副本送 `recorder.py` 录制；`zones.py` 的入侵检查线程周期读取检测结果判定报警；`routes.py` 与各模块注册的路由提供网页接口（含模型热切换与录像删除）。各模块导入时均无副作用（不加载模型、不连摄像头、不起线程）。
